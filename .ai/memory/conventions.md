# Conventions

- Runtime state is ephemeral; durable memory is curated Markdown.
- SMALL tasks default to one worker.
- Evidence from deterministic commands outranks agent assertions.
- Feature preview links must point to real artifacts.
